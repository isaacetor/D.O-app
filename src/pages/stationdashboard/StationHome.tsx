import React from "react";
import styled from "styled-components";

const StationHome = () => {
  return <Container>StationHome</Container>;
};

export default StationHome;
const Container = styled.div`
  width: calc(100% - 250px);
  background-color: red;
`;
